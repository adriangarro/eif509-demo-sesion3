spring.application.name=eif509-demo-sesion3

# Conexion a la base de la demo (mismo docker-compose.yml)
spring.datasource.url=jdbc:postgresql://localhost:5432/eif509
spring.datasource.username=dev
spring.datasource.password=dev

# Las migraciones las corremos con ./gradlew flywayMigrate (demo),
# pero si arrancan la app, Flyway tambien las aplica al inicio.
spring.flyway.enabled=true
